from django.contrib import admin
from propapp.models import plotPost
from propapp.models import contactInfo

# Register your models here.
admin.site.register(plotPost)
admin.site.register(contactInfo)
